var width = 500;
var height = 500;

function setup() {
  // Fixed screen size
  createCanvas(width, height);

  // Full screen
  // createCanvas(windowWidth, windowHeight);
}

function draw() {
  background('blue');
}
